package com.jonikoone.dictionaryforlearning.di

import org.koin.dsl.module

val viewModelModules = module {

    /*viewModel { (label: Label) -> ColorPickerViewModel(get<AppDatabase>().getLabelDao(), label) }


    viewModel { (label: Label) -> LabelViewModel(get(), label) }
    viewModel { LabelListViewModel(get()) }


    viewModel { (dictionaryId: Long?) -> WordsListViewModel(dictionaryId) }

    viewModel { (word: Word) -> WordViewModel(word) }*/


    //viewModel { DictionaryListViewModel() }


}